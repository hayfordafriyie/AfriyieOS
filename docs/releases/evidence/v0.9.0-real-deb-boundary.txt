What libafpkg does with packages dpkg itself built
==================================================

Generated by tools/pkgsynth.py's sibling: a real dpkg-deb package, to
establish exactly where the reader's boundary is rather than assuming it.

--- dpkg-deb default (what modern Debian and Ubuntu ship) ---
  members:
    debian-binary
    control.tar.zst
    data.tar.zst
  compression:
    control.tar.zst    Zstandard compressed data (v0.8+), Dictionary ID: None
    data.tar.zst       Zstandard compressed data (v0.8+), Dictionary ID: None
  reader says:
  status : -8 (refused)
  reason : control member uses a compression this build does not support (only control.tar and control.tar.gz are handled)

--- dpkg-deb -Zgzip (the format this build handles) ---
  members:
    debian-binary
    control.tar.gz
    data.tar.gz
  reader says:
  status : -8 (refused)
  reason : gzip stream uses Huffman-coded blocks, which this build does not inflate (only stored blocks are handled)

--- and the generated fixture, for comparison ---
  reader says:
  status : 0 (AF_OK)
  reason : read as a Debian package

CONCLUSION
  Real packages need two things this build does not have: full DEFLATE
  (Huffman-coded blocks) and Zstandard. Both are named in afpkg.h and in
  the compatibility plan rather than discovered by a user.
